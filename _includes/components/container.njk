{# ------------------------------------------------------------------------------------------------------------------------------------- #}

{% set sizeClasses =
    {
        "sm": "var(--container-sm)",
        "base": "var(--container-base)",
        "full": "full"
    }
 %}

{% set typeClasses =
    {
        "cols-1": "",
        "cols-2": "",
        "sidebar": "lg:grid-cols-[300px_1fr]"
    }
 %}

{# ------------------------------------------------------------------------------------------------------------------------------------- #}

{# {% macro section(props={}, attrs={})%}
    <{% if props.tag %}{{ props.tag }}{% else %}section{% endif %}
        {% include "snippets/loopAttrs.njk" %}
        class="w-full {{ attrs.class }}">
        {{ caller() | safe }}
    </{% if props.tag %}{{ props.tag }}{% else %}section{% endif %}>
{% endmacro %} #}

 {# ------------------------------------------------------------------------------------------------------------------------------------- #}

{% macro container(props={}, attrs={})%}
    <div
        {% include "snippets/loopAttrs.njk" %}
        class="zine-store-container mx-auto max-w-[{% if props.size %}{{sizeClasses[props.size]}}{% else %}{{ sizeClasses.base }}{% endif %}] {% if props.type %}{{ typeClasses[props.type] }}{% endif %} grid {{ attrs.class }}">
        {{ caller() | safe }}
    </div>
{% endmacro %}
