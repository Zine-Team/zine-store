---
layout: default

---
{% from "components/button.njk" import button %}
{% from "components/siteNav.njk" import siteNav %}
{% from "components/markdown.njk" import markdown %}

{% set custom %}
    b, strong {
        color: red;
    }
{% endset %}

{{ siteNav() }}
<main class="flex flex-col max-w-4xl zine-store-container items-start gap-site py-section grow w-full">
    <header class=""><h1 class="text-xl">{{ title }}</h1></header>
    {{ markdown({
        content: content,
        fontSize: "text-base",
        primaryColor: "color-black",
        linkColor: "color-link",
        radius: "radius",
        customStyles: custom
    }) }}
</main>
