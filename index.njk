---
layout: default
recentZines: Most recent zines
allZines: All zines (A-Z)
---
{% from "components/button.njk" import button %}

<main class="flex flex-col items-center gap-20 pb-20 grow w-full">
    <section class="zine-store-container flex flex-col gap-4">
        <h1 class="text-3xl">{{ global.siteTitle }}</h1>
        <p class="text-xl">{{ global.siteDescription }}</p>
    </section>
    <section class="zine-store-container flex flex-col gap-6">
        <h2 class="text-2xl">{{ recentZines }}</h2>
        <div class="zine-grid grow-1 grid grid-cols-2 gap-4 lg:grid-cols-4 lg:gap-8">
            {% for zine in collections.zine | reverse | slice(4) %}
                <a class="shadow-zine" href="{{ zine.url }}">
                    <img class="w-full shadow-page" src="{{ zine.data.pages[0] }}" alt="{{ zine.data.title }}"></a>
            {% endfor %}
        </div>
    </section>
    <section class="zine-store-container titled-section flex flex-col gap-6">
        <h2 class="text-2xl">{{ allZines }}</h2>
        <ul class="flex flex-col gap-8">
            {% for zine in collections.zine | sort(false, false, "data.title") %}
                <li>
                    {{button(label=zine.data.title, href=zine.url, type="link")}}
                </li>
            {% endfor %}
        </ul>
    </section>
</main>
