<nav class="container navbar">
    <!-- todo: make component? Used on multiple pages -->
    <span></span>
    <span>/index/</span>
</nav>
<main>
    <section class="container hero-section">
        <h1>Zine Store (Free)</h1>
        <p class="body-lg">A free store of zines for viewing and download</p>
    </section>
    <section class="container zine-gallery">
        {{#each zines}}
            <a class="zine" href="./zines/show.html?zine={{this.id}}">
                <img src="./zines/{{this.id}}/pages/page_cover.jpg"></img>
            </a>
        {{/each}}
    </section>
</main>
